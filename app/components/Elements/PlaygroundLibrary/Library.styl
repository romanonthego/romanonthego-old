.emptyState
  display: flex
  flex-direction: row
  justify-content: center
  align-items: center
  padding: x(20) x(10)
  background-color: rgba(white, 0.1)
  flex: 1 0 auto
  width: 100%
  text-align: center
